# Rate limit for webhook endpoint (http context; included before server blocks).
# Raised from 2r/s to 10r/s so GitHub's retry bursts and Cloudflare egress-IP
# collapsing don't trip 503s on legitimate push events.
limit_req_zone $binary_remote_addr zone=webhook_limit:10m rate=10r/s;
